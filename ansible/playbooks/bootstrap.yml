---
- name: Bootstrap Common Packages
  hosts: all
  become: true
  tasks:
    - name: Install base packages
      ansible.builtin.apt:
        name:
          - git
          - curl
          - unzip
        state: present
        update_cache: true
